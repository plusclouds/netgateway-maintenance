---
- name: Set Static IP Addresses
  template:
    src: interfaces.j2
    dest: /etc/network/interfaces

- name: install net-tools
  shell: apt update && apt install net-tools -y

- name: 
  shell: systemctl restart networking

- name: set mtu 9000
  shell: "{{ item }}"
  with_items:
    - ifconfig eth0 mtu 9000 up
    - ifconfig eth1 mtu 9000 up

- name: set hostname
  shell: hostnamectl set-hostname netgateway

- name: add myself to /etc/hosts
  lineinfile:
    dest: /etc/hosts
    regexp: '^127\.0\.0\.1[ \t]+localhost'
    line: '127.0.0.1 localhost netgateway'
    state: present

- set_fact:
   ip_hash: "{{ netgateway_meta.json.data.networkHash }}"

- debug:
   var: ip_hash

- set_fact:
   is_ipconfig_installed:  "{{ lookup('ini', 'ipconf section=installation file=new_installation.ini') }}"

- debug:
   var: is_ipconfig_installed