---
- name: Check if Proxy Config Exists
  stat: path=/etc/nginx/sites-enabled/{{ host }}.conf
  register: proxy_config

- name: Create Proxy Config
  file:
    state: absent
    path:  /etc/nginx/sites-enabled/{{ host }}.conf
  when: proxy_config.stat.exists == True
  notify: Reload Nginx